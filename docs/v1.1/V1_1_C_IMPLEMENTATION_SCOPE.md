# StayQR V1.1-C — Platform, Communication & Multi-Property

## Whole-batch scope
- Meta WhatsApp delivery remains consent/template/provider gated.
- Per-hotel channel settings add transactional/marketing controls.
- Per-hotel provider circuit health contains repeated provider failures.
- Group View aggregates only hotels already authorized to the signed-in user.
- Cross-property operational writes still require explicit hotel switching.
- Platform Admin View as Hotel is reduced to one concurrent audited session per admin and max 120 minutes.
- Support activity never extends the immutable expiry.
- Production provider secrets stay only in Edge Function environment variables.
- Production remains excluded until explicit V1.1 release authorization.
