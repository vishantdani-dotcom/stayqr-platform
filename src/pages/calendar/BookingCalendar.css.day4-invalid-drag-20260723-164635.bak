.booking-calendar-page {
  min-height: 100%;
  padding: 24px;
  color: #f8f6ed;
  background:
    radial-gradient(circle at 95% 0%, rgba(212, 175, 55, 0.08), transparent 30%),
    #0b0b0b;
}

.calendar-page-header,
.calendar-toolbar,
.calendar-filters,
.calendar-legend,
.calendar-workspace,
.calendar-pagination {
  max-width: 1600px;
  margin-left: auto;
  margin-right: auto;
}

.calendar-page-header {
  display: flex;
  align-items: flex-start;
  justify-content: space-between;
  gap: 20px;
  margin-bottom: 20px;
}

.calendar-page-header h1 {
  margin: 2px 0 6px;
  font-size: clamp(26px, 3vw, 40px);
  letter-spacing: -0.04em;
  color: #fff;
}

.calendar-page-header p:not(.calendar-eyebrow) {
  margin: 0;
  color: #979797;
  font-size: 13px;
}

.calendar-eyebrow {
  margin: 0;
  color: #d4af37;
  font-size: 11px;
  font-weight: 800;
  letter-spacing: 0.12em;
  text-transform: uppercase;
}

.calendar-header-actions,
.calendar-period-controls,
.calendar-view-switch,
.calendar-pagination > div:last-child,
.calendar-details-drawer footer,
.calendar-modal footer {
  display: flex;
  align-items: center;
  gap: 10px;
}

.calendar-btn,
.calendar-period-controls button,
.calendar-view-switch button,
.calendar-clear-filters,
.historical-block-filter button,
.calendar-pagination button,
.calendar-details-drawer button,
.calendar-modal button,
.unallocated-card button {
  border: 1px solid #2d2d2d;
  border-radius: 9px;
  background: #151515;
  color: #f7f7f7;
  padding: 10px 14px;
  cursor: pointer;
  font: inherit;
  font-size: 12px;
  font-weight: 700;
  transition: 160ms ease;
}

.calendar-btn:hover,
.calendar-period-controls button:hover,
.calendar-view-switch button:hover,
.calendar-clear-filters:hover,
.historical-block-filter button:hover,
.calendar-pagination button:hover,
.calendar-details-drawer button:hover,
.calendar-modal button:hover,
.unallocated-card button:hover {
  border-color: #8e762a;
  transform: translateY(-1px);
}

.calendar-btn.primary,
.calendar-details-drawer button.primary,
.calendar-modal button.primary {
  background: #d4af37;
  border-color: #d4af37;
  color: #080808;
}

.calendar-details-drawer button.danger {
  color: #ff7e7e;
  border-color: rgba(255, 82, 82, 0.5);
  background: rgba(255, 82, 82, 0.08);
}

.calendar-btn:disabled,
.calendar-pagination button:disabled,
.calendar-modal button:disabled {
  opacity: 0.45;
  cursor: not-allowed;
  transform: none;
}

.calendar-toolbar {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 18px;
  border: 1px solid #262626;
  border-radius: 14px;
  background: #111;
  padding: 12px;
  margin-bottom: 12px;
}

.calendar-period-controls input,
.calendar-filters input,
.calendar-filters select,
.calendar-modal input,
.calendar-modal select,
.calendar-modal textarea {
  border: 1px solid #303030;
  border-radius: 8px;
  background: #111;
  color: #fff;
  min-height: 40px;
  padding: 9px 11px;
  font: inherit;
  color-scheme: dark;
}

.calendar-period-controls strong {
  margin-left: 4px;
  color: #e8e8e8;
  white-space: nowrap;
  font-size: 13px;
}

.calendar-period-controls button {
  min-width: 38px;
  padding: 9px;
}

.calendar-period-controls .today {
  min-width: auto;
  padding-left: 14px;
  padding-right: 14px;
}

.calendar-view-switch {
  border: 1px solid #292929;
  background: #090909;
  padding: 4px;
  border-radius: 10px;
}

.calendar-view-switch button {
  border: 0;
  padding: 8px 16px;
  background: transparent;
}

.calendar-view-switch button.active {
  color: #070707;
  background: #d4af37;
}

.calendar-filters {
  display: grid;
  grid-template-columns: minmax(180px, 0.8fr) minmax(180px, 0.8fr) minmax(360px, 2fr) auto auto;
  gap: 12px;
  align-items: end;
  border: 1px solid #242424;
  border-radius: 14px;
  background: #111;
  padding: 14px;
  margin-bottom: 12px;
}

.calendar-filters > label,
.calendar-status-filter,
.calendar-modal label {
  display: flex;
  flex-direction: column;
  gap: 7px;
}

.calendar-filters span,
.calendar-status-filter > span,
.calendar-modal label > span {
  color: #a6a6a6;
  font-size: 10px;
  font-weight: 800;
  letter-spacing: 0.06em;
  text-transform: uppercase;
}

.calendar-status-filter > div {
  display: flex;
  flex-wrap: wrap;
  gap: 6px;
}

.calendar-status-filter label {
  display: inline-flex;
  align-items: center;
  gap: 6px;
  border: 1px solid #2a2a2a;
  border-radius: 999px;
  padding: 8px 10px;
  color: #b7b7b7;
  font-size: 11px;
  cursor: pointer;
}

.calendar-status-filter label.selected {
  border-color: rgba(212, 175, 55, 0.75);
  background: rgba(212, 175, 55, 0.11);
  color: #f5d777;
}

.calendar-status-filter input {
  min-height: auto;
  accent-color: #d4af37;
}

.historical-block-filter button.active {
  border-color: rgba(212, 175, 55, 0.7);
  color: #f5d777;
}

.calendar-clear-filters {
  align-self: end;
  white-space: nowrap;
}

.calendar-legend {
  display: flex;
  align-items: center;
  flex-wrap: wrap;
  gap: 9px 14px;
  border: 1px solid #242424;
  border-radius: 12px;
  background: #0e0e0e;
  padding: 11px 14px;
  margin-bottom: 14px;
}

.calendar-legend > span {
  display: inline-flex;
  align-items: center;
  gap: 7px;
  color: #a6a6a6;
  font-size: 11px;
}

.legend-dot {
  width: 9px;
  height: 9px;
  border-radius: 3px;
  background: #777;
  box-shadow: 0 0 0 1px rgba(255,255,255,0.12);
}

.legend-dot.draft,
.calendar-event.draft,
.calendar-status-chip.draft { background: #4a4a4a; }
.legend-dot.tentative,
.calendar-event.tentative,
.calendar-status-chip.tentative { background: #826f2d; }
.legend-dot.confirmed,
.calendar-event.confirmed,
.calendar-status-chip.confirmed { background: #247a4a; }
.legend-dot.checked_in,
.calendar-event.checked_in,
.calendar-status-chip.checked_in { background: #2e69a5; }
.legend-dot.checked_out,
.calendar-event.checked_out,
.calendar-status-chip.checked_out { background: #5b5b73; }
.legend-dot.cancelled,
.calendar-event.cancelled,
.calendar-status-chip.cancelled { background: #7d2f34; }
.legend-dot.no_show,
.calendar-event.no_show,
.calendar-status-chip.no_show { background: #692d66; }
.legend-dot.block-maintenance,
.calendar-event.block-maintenance { background: #a25c28; }
.legend-dot.block-operational,
.calendar-event.block-operational { background: #896b22; }
.legend-dot.block-out_of_order,
.calendar-event.block-out_of_order { background: #a43737; }
.legend-dot.block-owner_use,
.calendar-event.block-owner_use { background: #60427d; }
.legend-dot.block-deep_cleaning,
.calendar-event.block-deep_cleaning { background: #2c7770; }
.legend-dot.block-other,
.calendar-event.block-other { background: #5b5b5b; }
.legend-dot.direct_stay,
.calendar-event.direct_stay { background: #285f8f; }

.calendar-workspace {
  display: grid;
  grid-template-columns: minmax(0, 1fr) 310px;
  gap: 14px;
  align-items: start;
}

.calendar-timeline-scroll {
  overflow: auto;
  max-height: calc(100vh - 400px);
  min-height: 430px;
  border: 1px solid #272727;
  border-radius: 14px;
  background: #0d0d0d;
}

.calendar-timeline {
  position: relative;
}

.calendar-grid-header,
.calendar-room-row {
  display: grid;
  grid-template-columns: 220px minmax(0, 1fr);
}

.calendar-grid-header {
  position: sticky;
  top: 0;
  z-index: 30;
  border-bottom: 1px solid #303030;
  background: #121212;
}

.calendar-room-header,
.calendar-room-meta {
  position: sticky;
  left: 0;
  z-index: 22;
  border-right: 1px solid #303030;
  background: #121212;
}

.calendar-room-header {
  display: flex;
  align-items: center;
  padding: 14px;
  color: #d7d7d7;
  font-size: 11px;
  font-weight: 800;
  text-transform: uppercase;
  letter-spacing: 0.08em;
}

.calendar-date-header-grid,
.calendar-room-track {
  display: grid;
  min-width: 0;
}

.calendar-date-header-grid > div {
  min-height: 64px;
  border-right: 1px solid #292929;
  display: flex;
  flex-direction: column;
  justify-content: center;
  align-items: center;
  gap: 3px;
}

.calendar-date-header-grid > div.today {
  background: rgba(212, 175, 55, 0.1);
}

.calendar-date-header-grid span {
  color: #777;
  font-size: 10px;
  text-transform: uppercase;
}

.calendar-date-header-grid strong {
  font-size: 16px;
}

.calendar-room-row {
  min-height: 82px;
  border-bottom: 1px solid #222;
}

.calendar-room-meta {
  padding: 13px 14px;
  display: flex;
  flex-direction: column;
  justify-content: center;
  gap: 4px;
}

.calendar-room-meta strong {
  font-size: 13px;
}

.calendar-room-meta span {
  color: #929292;
  font-size: 11px;
}

.room-operational-status {
  width: fit-content;
  padding: 3px 7px;
  border-radius: 999px;
  background: #202020;
  color: #aaa;
  font-size: 9px;
  text-transform: uppercase;
}

.room-operational-status.available,
.room-operational-status.occupied { color: #9fddb5; }
.room-operational-status.maintenance,
.room-operational-status.out_of_order { color: #ff9b9b; }

.calendar-room-track {
  position: relative;
  grid-auto-rows: 82px;
}

.calendar-drop-cell {
  position: relative;
  grid-row: 1;
  min-width: 0;
  height: 82px;
  border: 0;
  border-right: 1px solid #222;
  background: transparent;
  cursor: cell;
}

.calendar-drop-cell:hover,
.calendar-drop-cell:focus-visible {
  background: rgba(212, 175, 55, 0.08);
  outline: 1px solid rgba(212, 175, 55, 0.35);
  outline-offset: -3px;
}

.calendar-drop-cell.drag-target {
  z-index: 12;
  background: rgba(212, 175, 55, 0.18);
  outline: 2px dashed #e4be3f;
  outline-offset: -4px;
}

.calendar-drop-cell.drag-target.current-target {
  background: rgba(255, 102, 102, 0.1);
  outline-color: #ff8787;
}

.calendar-drop-target-label {
  position: absolute;
  inset: 5px;
  z-index: 20;
  display: flex;
  flex-direction: column;
  justify-content: center;
  align-items: center;
  gap: 2px;
  border-radius: 7px;
  background: rgba(9, 9, 9, 0.9);
  color: #fff;
  pointer-events: none;
  text-align: center;
}

.calendar-drop-target-label strong {
  color: #f4cf52;
  font-size: 10px;
}

.calendar-drop-target-label small {
  color: #c8c8c8;
  font-size: 8px;
  line-height: 1.25;
}

.calendar-drop-cell.today {
  background: rgba(212, 175, 55, 0.035);
}

.calendar-event {
  grid-row: 1;
  align-self: center;
  z-index: 8;
  position: relative;
  display: flex;
  align-items: stretch;
  min-width: 0;
  height: 54px;
  margin: 0 4px;
  padding: 0;
  border: 1px solid rgba(255,255,255,0.16);
  border-radius: 9px;
  color: #fff;
  overflow: hidden;
  text-align: left;
  box-shadow: 0 7px 18px rgba(0,0,0,0.28);
}

.calendar-event-content {
  flex: 1;
  min-width: 0;
  border: 0;
  background: transparent;
  color: inherit;
  padding: 8px 10px;
  text-align: left;
  cursor: pointer;
}

.calendar-event-content:hover,
.calendar-event-content:focus-visible {
  background: rgba(255, 255, 255, 0.06);
  outline: none;
}

.calendar-event.non-inventory {
  opacity: 0.55;
  background-image: repeating-linear-gradient(135deg, transparent, transparent 6px, rgba(255,255,255,0.1) 6px, rgba(255,255,255,0.1) 9px);
}

.calendar-event-content strong,
.calendar-event-content span {
  display: block;
  overflow: hidden;
  white-space: nowrap;
  text-overflow: ellipsis;
}

.calendar-event-content strong { font-size: 11px; }
.calendar-event-content span { margin-top: 4px; font-size: 9px; opacity: 0.78; text-transform: uppercase; }

.calendar-drag-handle {
  flex: 0 0 30px;
  display: grid;
  place-items: center;
  border-left: 1px solid rgba(255, 255, 255, 0.16);
  background: rgba(0, 0, 0, 0.22);
  color: rgba(255, 255, 255, 0.88);
  font-size: 13px;
  letter-spacing: -3px;
  cursor: grab;
  user-select: none;
}

.calendar-drag-handle:hover,
.calendar-drag-handle:focus-visible {
  background: rgba(0, 0, 0, 0.42);
  color: #fff;
  outline: 1px solid rgba(244, 207, 82, 0.75);
  outline-offset: -2px;
}

.calendar-drag-handle:active {
  cursor: grabbing;
}

.calendar-drag-preview {
  position: fixed;
  top: -1000px;
  left: -1000px;
  z-index: 99999;
  width: 190px;
  border: 1px solid #d4af37;
  border-radius: 9px;
  background: #17130a;
  color: #fff;
  padding: 9px 12px;
  box-shadow: 0 12px 30px rgba(0, 0, 0, 0.55);
  font-size: 11px;
  font-weight: 800;
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
}

.calendar-empty-row {
  padding: 60px 20px;
  text-align: center;
  color: #777;
}

.unallocated-panel {
  position: sticky;
  top: 85px;
  border: 1px solid #292929;
  border-radius: 14px;
  background: #111;
  overflow: hidden;
}

.unallocated-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  border-bottom: 1px solid #262626;
  padding: 15px;
}

.unallocated-header p { margin: 0 0 3px; color: #d4af37; font-size: 9px; font-weight: 800; text-transform: uppercase; letter-spacing: .1em; }
.unallocated-header h2 { margin: 0; color: #fff; font-size: 16px; }
.unallocated-header > span { display: grid; place-items: center; width: 32px; height: 32px; border-radius: 10px; background: rgba(212,175,55,.15); color: #f6d96e; font-weight: 800; }
.unallocated-help { margin: 0; padding: 11px 15px; color: #777; font-size: 10px; border-bottom: 1px solid #222; }
.unallocated-list { max-height: calc(100vh - 500px); min-height: 250px; overflow-y: auto; padding: 10px; }
.unallocated-empty { padding: 55px 15px; text-align: center; color: #777; font-size: 12px; }
.unallocated-card { border: 1px solid #2b2b2b; border-radius: 11px; background: #161616; padding: 12px; margin-bottom: 9px; cursor: grab; }
.unallocated-card > div { display: flex; justify-content: space-between; gap: 8px; align-items: center; }
.unallocated-card > div > strong { color: #f5d363; font-size: 11px; }
.unallocated-card h3 { margin: 10px 0 4px; font-size: 13px; }
.unallocated-card p { margin: 0 0 4px; color: #999; font-size: 11px; }
.unallocated-card small { display: block; color: #777; font-size: 10px; }
.unallocated-card button { width: 100%; margin-top: 10px; padding: 8px; color: #f5d363; }
.calendar-status-chip { padding: 4px 7px; border-radius: 999px; color: #fff; font-size: 8px; text-transform: uppercase; }

.calendar-pagination {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 14px 0 2px;
  color: #888;
  font-size: 11px;
}

.calendar-inline-error,
.calendar-form-error {
  max-width: 1600px;
  margin: 0 auto 12px;
  border: 1px solid rgba(255,82,82,.45);
  border-radius: 10px;
  background: rgba(255,82,82,.08);
  color: #ff9d9d;
  padding: 11px 13px;
  font-size: 12px;
}

.calendar-toast {
  position: fixed;
  top: 82px;
  right: 20px;
  z-index: 1000;
  max-width: 430px;
  border: 1px solid #2b2b2b;
  border-radius: 10px;
  background: #151515;
  padding: 12px 16px;
  box-shadow: 0 15px 40px rgba(0,0,0,.5);
  font-size: 12px;
}

.calendar-toast.success { border-color: rgba(65,180,105,.5); color: #9ce0b3; }
.calendar-toast.error { border-color: rgba(255,82,82,.5); color: #ff9c9c; }

.calendar-drawer-backdrop,
.calendar-modal-backdrop {
  position: fixed;
  inset: 0;
  z-index: 800;
  background: rgba(0,0,0,.67);
  backdrop-filter: blur(5px);
}

.calendar-details-drawer {
  position: absolute;
  top: 0;
  right: 0;
  width: min(440px, 100%);
  height: 100%;
  border-left: 1px solid #303030;
  background: #0e0e0e;
  display: flex;
  flex-direction: column;
  box-shadow: -20px 0 60px rgba(0,0,0,.45);
}

.calendar-details-drawer > header,
.calendar-modal > header {
  display: flex;
  justify-content: space-between;
  align-items: flex-start;
  gap: 20px;
  border-bottom: 1px solid #292929;
  padding: 20px;
}

.calendar-details-drawer h2,
.calendar-modal h2 { margin: 4px 0 0; color: #fff; font-size: 21px; }
.calendar-details-drawer header > button,
.calendar-modal header > button { width: 34px; height: 34px; padding: 0; font-size: 22px; }
.calendar-details-body { flex: 1; overflow-y: auto; padding: 16px; }
.calendar-large-status { display: inline-block; border-radius: 999px; padding: 7px 11px; color: #fff; font-size: 10px; font-weight: 800; text-transform: uppercase; }
.calendar-detail-card { display: grid; grid-template-columns: 1fr 1fr; gap: 1px; border: 1px solid #292929; border-radius: 12px; overflow: hidden; margin-top: 14px; background: #292929; }
.calendar-detail-card > div { background: #151515; padding: 13px; min-height: 66px; }
.calendar-detail-card > div.wide { grid-column: 1 / -1; }
.calendar-detail-card span { display: block; color: #777; font-size: 9px; text-transform: uppercase; margin-bottom: 7px; }
.calendar-detail-card strong { display: block; font-size: 11px; line-height: 1.5; word-break: break-word; }
.calendar-details-drawer footer { flex-wrap: wrap; border-top: 1px solid #292929; padding: 14px; }

.calendar-modal {
  position: absolute;
  top: 50%;
  left: 50%;
  width: min(720px, calc(100% - 28px));
  max-height: calc(100vh - 30px);
  transform: translate(-50%, -50%);
  border: 1px solid #303030;
  border-radius: 15px;
  background: #0f0f0f;
  overflow: auto;
  box-shadow: 0 25px 80px rgba(0,0,0,.65);
}

.calendar-modal.compact { width: min(520px, calc(100% - 28px)); }
.calendar-modal-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 13px; padding: 20px; }
.calendar-modal-grid label.wide { grid-column: 1 / -1; }
.allocation-summary.wide { grid-column: 1 / -1; }
.allocation-summary {
  border: 1px solid #2d2d2d;
  border-radius: 9px;
  background: #151515;
  padding: 12px;
}
.allocation-summary span { display: block; color: #8c8c8c; font-size: 9px; text-transform: uppercase; letter-spacing: .07em; }
.allocation-summary strong { display: block; margin-top: 6px; color: #fff; font-size: 13px; }
.allocation-summary small { display: block; margin-top: 4px; color: #a7a7a7; font-size: 10px; }
.calendar-modal textarea { min-height: 95px; resize: vertical; }
.calendar-modal footer { justify-content: flex-end; border-top: 1px solid #292929; padding: 14px 20px; }
.calendar-modal .calendar-form-error { margin: 0 20px 14px; }
.allocation-description { margin: 18px 20px 0; color: #bbb; font-size: 12px; }
.allocation-warning { margin: 0 20px 18px; border: 1px solid rgba(212,175,55,.28); border-radius: 9px; background: rgba(212,175,55,.07); color: #bfae78; padding: 11px; font-size: 10px; line-height: 1.5; }

.calendar-state {
  min-height: calc(100vh - 90px);
  display: grid;
  place-content: center;
  text-align: center;
  background: #0b0b0b;
  color: #fff;
}
.calendar-state > div { font-size: 34px; }
.calendar-state h2 { margin: 12px 0 5px; }
.calendar-state p { margin: 0; color: #888; }

@media (max-width: 1200px) {
  .calendar-filters { grid-template-columns: 1fr 1fr; }
  .calendar-status-filter { grid-column: 1 / -1; }
  .calendar-workspace { grid-template-columns: 1fr; }
  .unallocated-panel { position: static; }
  .unallocated-list { max-height: 330px; }
}

@media (max-width: 760px) {
  .booking-calendar-page { padding: 16px 12px; }
  .calendar-page-header,
  .calendar-toolbar { align-items: stretch; flex-direction: column; }
  .calendar-header-actions { width: 100%; }
  .calendar-header-actions button { flex: 1; }
  .calendar-period-controls { flex-wrap: wrap; }
  .calendar-period-controls strong { width: 100%; }
  .calendar-view-switch { align-self: stretch; }
  .calendar-view-switch button { flex: 1; }
  .calendar-filters { grid-template-columns: 1fr; }
  .calendar-status-filter { grid-column: auto; }
  .calendar-grid-header,
  .calendar-room-row { grid-template-columns: 170px minmax(0, 1fr); }
  .calendar-room-header,
  .calendar-room-meta { width: 170px; }
  .calendar-modal-grid { grid-template-columns: 1fr; }
  .calendar-modal-grid label.wide { grid-column: auto; }
  .allocation-summary.wide { grid-column: auto; }
  .calendar-detail-card { grid-template-columns: 1fr; }
  .calendar-detail-card > div.wide { grid-column: auto; }
  .calendar-pagination { align-items: stretch; flex-direction: column; gap: 10px; }
}
